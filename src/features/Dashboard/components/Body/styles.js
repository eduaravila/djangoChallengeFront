import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  mapContainer: {
    width: "100%",
    height: "100vh",
    margin: "auto",
    position: "absolute",
  },
}));
